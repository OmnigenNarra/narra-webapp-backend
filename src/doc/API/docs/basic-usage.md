1.  Upload JSON (see **`./examples/`**) through
    **`/admin/api/package/add/`** (**`API` &rarr; `Packages` &rarr;
    `Add`**) or **`POST`**-ing directly to **`/api/v1/package`**.
2.  Fetch JSON via **`/api/v1/package/{package_id}`** (**`GET`**).
3.  Update package through **`/api/v1/package/{package_id}`** (**`PATCH`** or **`PUT`**)
